{
  "name": "@ai-sdlc/root",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:e2e": "vitest run tests/",
    "lint": "pnpm -r run lint",
    "lint:arch": "npx tsx scripts/lint-arch-rules.ts",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "tsx": "^4.22.0",
    "typescript": "5.7.3",
    "vitest": "^4.1.6"
  }
}
